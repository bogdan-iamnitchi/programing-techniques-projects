package dao;

import model.Client;

/**
 * Clasa ClientDAO extinde clasa AbstractDAO și furnizează interogări specifice pentru tabela "Client".
 * Implementează operațiile CRUD de bază moștenite de la clasa părinte.
 */
public class ClientDAO extends AbstractDAO<Client> {

    // uses basic CRUD methods from superclass

    // TODO: create only student specific queries
}
